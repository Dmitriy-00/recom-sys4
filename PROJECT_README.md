# Комплексная Рекомендательная Система для Трансформативных Медиа

Полная реализация алгоритма из документа **"Максимально полный алгоритм рекомендаций трансформативных медиа"**.

## 🎯 Особенности

### Основной Алгоритм

Система реализует **state-of-the-art** подход к рекомендациям с 8 компонентами скоринга:

1. **Trope-Based Scoring (25%)** - Рекомендации на основе тропов и их использования
2. **Content-Based Scoring (20%)** - Тематическое и жанровое соответствие
3. **Structural-Based Scoring (20%)** - Сложность, темп, структура
4. **Transformation-Based (15%)** - Трансформативный потенциал
5. **Collaborative Filtering (10%)** - Схожие пользователи
6. **Popularity-Based (5%)** - Культурная значимость
7. **Context-Aware (5%)** - Контекст и состояние пользователя
8. **Serendipity (5%)** - Элемент неожиданности

### Модель Данных

#### Материалы (89+ параметров):
- Базовые метаданные
- Жанровая таксономия
- Визуальные характеристики (для фильмов)
- Текстовые характеристики (для книг)
- Аудио параметры
- Темпоритм и структура
- Персонажи
- Тематика и философская глубина
- Эмоциональные параметры
- Тропы и их использование
- Когнитивные операции
- Трансформативный потенциал
- Культурный контекст
- Параметры аудитории
- Факторы сложности

#### Пользователи (многомерный профиль):
- Демографические данные
- Когнитивный профиль
- Жанровая грамотность
- Профиль работы с тропами
- Тематические предпочтения
- Эмоциональный профиль
- Цели и мотивация
- Временные ресурсы
- История взаимодействий

### Карта Тропов

Граф знаний, где:
- **Узлы** = тропы и материалы
- **Рёбра** = использование тропов и влияния между материалами
- Поддержка эволюции тропов во времени
- Кластерный анализ связанных тропов
- Персонализированные пути исследования

### Ключевые Концепции

#### Зона Ближайшего Развития (ZPD)
Система приоритизирует материалы в зоне ближайшего развития пользователя - не слишком простые и не слишком сложные.

#### Работа с Тропами
Различает 5 типов использования тропов:
- **Straight** - прямое использование
- **Deconstruction** - деконструкция
- **Subversion** - субверсия
- **Reconstruction** - реконструкция
- **Meta** - мета-комментарий

#### Трансформативное Обучение
Фокус на материалах с высоким трансформативным потенциалом, которые могут изменить мировоззрение пользователя.

## 🚀 Установка

```bash
# Клонировать репозиторий
git clone https://github.com/your-repo/recom-sys4.git
cd recom-sys4

# Установить зависимости
pip install -r requirements.txt

# Установить пакет
pip install -e .
```

## 📖 Использование

### Базовый Пример

```python
from recom_system import RecommendationEngine, Material, User
from recom_system.models import MaterialMetadata, MaterialType, SessionContext

# Создать движок
engine = RecommendationEngine()

# Добавить материалы
material = Material(
    id="matrix_1999",
    metadata=MaterialMetadata(
        title="The Matrix",
        year=1999,
        creator="Wachowski Sisters",
        type=MaterialType.FILM
    )
)
engine.add_material(material)

# Создать пользователя
user = User(id="user_001", username="alice")
engine.add_user(user)

# Получить рекомендации
recommendations = engine.get_recommendations(
    user_id="user_001",
    session_context=SessionContext(),
    top_k=10
)

for rec in recommendations:
    print(f"{rec['material'].metadata.title}: {rec['final_score']:.2f}/10")
    print(f"  {rec['explanation']}")
```

### Полный Пример

См. `examples/basic_usage.py` для полного примера использования всех возможностей системы.

```bash
python examples/basic_usage.py
```

## 📊 Архитектура

```
recom_sys4/
├── recom_system/
│   ├── __init__.py
│   ├── models.py                    # Модели данных (Material, User, Trope)
│   ├── trope_map.py                 # Система карты тропов
│   ├── scoring_components.py        # 8 компонентов скоринга
│   └── recommendation_engine.py     # Главный движок
│
├── examples/
│   └── basic_usage.py              # Пример использования
│
├── data/                           # Исходные данные
│   ├── films_master_list_extended.md
│   ├── books_master_list_extended.md
│   ├── tropes_master_list.md
│   └── materials_master_list.md
│
├── docs/                           # Документация
│   ├── Максимально полный алгоритм рекомендаций трансформативных медиа.md
│   ├── trope_map_documentation.md
│   └── registry_*.md
│
├── requirements.txt
└── README.md
```

## 🔧 API Reference

### RecommendationEngine

#### Основные Методы

```python
# Получить рекомендации
recommendations = engine.get_recommendations(
    user_id: str,
    session_context: Optional[SessionContext] = None,
    top_k: int = 20,
    filters: Optional[Dict[str, Any]] = None
)

# Записать оценку
engine.record_rating(
    user_id: str,
    material_id: str,
    rating: Rating
)

# Получить пути исследования
paths = engine.get_exploration_paths(
    user_id: str,
    liked_material_id: str
)

# Идентифицировать пробелы в знаниях
gaps = engine.identify_knowledge_gaps(user_id: str)

# Анализ профиля пользователя
analysis = engine.analyze_user_profile(user_id: str)
```

### TropeMap

```python
# Добавить троп
trope_map.add_trope(trope: Trope)

# Добавить материал
trope_map.add_material(material: Material)

# Построить путь исследования
paths = trope_map.build_exploration_path(
    user_current_level: float,
    liked_material_id: str,
    user_meta_awareness: float = 5.0
)

# Получить эволюцию тропа
evolution = trope_map.get_trope_evolution(
    trope_id: str,
    start_year: int = 1900,
    end_year: int = 2024
)

# Найти связанные тропы
related = trope_map.get_often_paired_tropes(
    trope_id: str,
    min_strength: float = 0.5
)
```

## 📈 Метрики и Оценка

Система отслеживает:
- **RMSE** - Root Mean Square Error предсказаний
- **Precision@K** - Точность топ-K рекомендаций
- **Diversity** - Разнообразие рекомендаций
- **Coverage** - Покрытие каталога
- **Novelty** - Новизна рекомендаций
- **Serendipity** - Неожиданные открытия
- **ZPD Hit Rate** - Попадание в зону роста

## 🎓 Педагогические Применения

### Построение Учебных Программ

```python
# Найти материалы для постепенного роста
path = engine.get_exploration_paths(user_id, starting_material_id)

for step in path:
    if step['type'] == 'deepening':
        # Углубление понимания тропа
        print(f"Следующий шаг: {step['rationale']}")
```

### Анализ Пробелов

```python
# Найти важные концепции, которые пользователь не знает
gaps = engine.identify_knowledge_gaps(user_id)

for gap in gaps:
    print(f"Рекомендуем изучить: {gap['trope'].name}")
    print(f"Начать с: {gap['recommended_intro']['material'].metadata.title}")
```

## 🔬 Научная Основа

Система основана на:
- **Zone of Proximal Development** (Vygotsky, 1978)
- **Transformative Learning Theory** (Mezirow, 1991)
- **Story Shape Analysis** (Vonnegut, Reagan et al.)
- **Collaborative Filtering** (Matrix Factorization)
- **Maximum Marginal Relevance** для diversity
- **Cultural-Historical Activity Theory**

## 📚 Данные

Система поддерживает:
- ✅ Фильмы (500+ в базовом списке)
- ✅ Книги (300+ в базовом списке)
- ✅ Тропы (500+ категорий)
- 🔄 Игры (в разработке)
- 🔄 Сериалы (в разработке)

## 🛠️ Разработка

### Добавление Нового Материала

```python
from recom_system.models import *

material = Material(
    id="unique_id",
    metadata=MaterialMetadata(
        title="Title",
        year=2024,
        creator="Creator Name",
        type=MaterialType.FILM
    )
)

# Установить параметры
material.difficulty.difficulty_level = 7.0
material.transformative.transformative_score = 8.0

# Добавить тропы
material.tropes.append(TropeUsage(
    trope_id="some_trope",
    trope_name="Some Trope",
    usage_type=UsageType.DECONSTRUCTION,
    execution=9.0,
    centrality=0.8
))

# Добавить в систему
engine.add_material(material)
```

### Расширение Системы

Компоненты скоринга можно расширять:

```python
from recom_system.scoring_components import ContentBasedScoring

class CustomContentScoring(ContentBasedScoring):
    @staticmethod
    def score(material, user):
        # Кастомная логика
        pass
```

## ⚙️ Настройка

### Веса Компонентов

Можно изменить базовые веса в `recommendation_engine.py`:

```python
base_weights = {
    'trope_based': 0.25,
    'content_based': 0.20,
    'structural_based': 0.20,
    'transformation_based': 0.15,
    # ... и т.д.
}
```

### Фильтры Безопасности

Настроить в профиле пользователя:

```python
user.emotional.avoid_triggers = ['violence', 'gore']
user.emotional.max_emotional_intensity = 8.0
```

## 🧪 Тестирование

```bash
# Запустить базовый пример
python examples/basic_usage.py

# Проверить импорты
python -c "from recom_system import RecommendationEngine; print('OK')"
```

## 📝 TODO / Roadmap

- [ ] Парсеры для автоматической загрузки данных из markdown
- [ ] ML-based embeddings для тропов
- [ ] Web API (Flask/FastAPI)
- [ ] Frontend (React/Vue)
- [ ] Поддержка мультимодальных рекомендаций
- [ ] A/B тестирование
- [ ] Экспорт в Obsidian
- [ ] Интеграция с внешними API (IMDb, Goodreads)
- [ ] Больше метрик и визуализаций

## 🤝 Вклад

Приветствуются Pull Requests! Особенно нужны:
- Больше материалов с детальными параметрами
- Улучшенные эмбеддинги для тропов
- Тесты
- Документация
- Примеры использования

## 📄 Лицензия

MIT License - см. LICENSE файл

## 🙏 Благодарности

Основано на теоретической работе:
- "Максимально полный алгоритм рекомендаций трансформативных медиа"
- Концепции карты тропов
- Списки материалов и тропов из репозитория

## 📞 Контакты

- Issues: [GitHub Issues](https://github.com/your-repo/recom-sys4/issues)
- Discussions: [GitHub Discussions](https://github.com/your-repo/recom-sys4/discussions)

---

**Версия:** 1.0.0
**Статус:** Alpha
**Дата:** 2025-01-18
